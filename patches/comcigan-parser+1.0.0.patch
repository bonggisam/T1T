diff --git a/node_modules/comcigan-parser/index.js b/node_modules/comcigan-parser/index.js
index caa9e40..0623b33 100644
--- a/node_modules/comcigan-parser/index.js
+++ b/node_modules/comcigan-parser/index.js
@@ -50,8 +50,9 @@ class Timetable {
 
     await new Promise((resolve, reject) => {
       request(HOST, (err, _res, body) => {
-        if (err) {
-          reject(err);
+        if (err || !body) {
+          reject(err || new Error('컴시간 응답 없음'));
+          return;
         }
 
         const frame = body
@@ -84,8 +85,9 @@ class Timetable {
           encoding: null,
         },
         (err, _res, body) => {
-          if (err) {
-            reject(err);
+          if (err || !body) {
+            reject(err || new Error('컴시간 응답 없음'));
+            return;
           }
 
           const source = iconv.decode(body, 'EUC-KR');
@@ -144,15 +146,23 @@ class Timetable {
 
     return new Promise((resolve, reject) => {
       request(this._baseUrl + this._extractCode + hexString, (err, _res, body) => {
-        let jsonString = body.substr(0, body.lastIndexOf('}') + 1);
-        let searchData = JSON.parse(jsonString)['학교검색'];
+        if (err || !body) {
+          reject(err || new Error('컴시간 응답 없음'));
+          return;
+        }
 
-        if (err) {
-          reject(err);
+        let jsonString = body.substr(0, body.lastIndexOf('}') + 1);
+        let searchData;
+        try {
+          searchData = JSON.parse(jsonString)['학교검색'];
+        } catch (e) {
+          reject(new Error('학교 검색 응답 파싱 실패'));
+          return;
         }
 
-        if (searchData.length <= 0) {
+        if (!Array.isArray(searchData) || searchData.length <= 0) {
           reject(new Error('검색된 학교가 없습니다.'));
+          return;
         }
 
         resolve(
@@ -266,12 +276,9 @@ class Timetable {
     // JSON 데이터 로드
     const jsonString = await new Promise((resolve, reject) => {
       request(this._baseUrl + sc3, (err, _ㄴres, body) => {
-        if (err) {
-          reject(err);
-        }
-
-        if (!body) {
-          reject(new Error('시간표 데이터를 찾을 수 없습니다.'));
+        if (err || !body) {
+          reject(err || new Error('시간표 데이터를 찾을 수 없습니다.'));
+          return;
         }
 
         // String to JSON
@@ -293,7 +300,9 @@ class Timetable {
   _getClassTimetable(codeConfig, grade, classNumber) {
     const args = [codeConfig.data, grade, classNumber];
     const call = codeConfig.functioName + '(' + args.join(',') + ')';
-    const script = codeConfig.script + '\n\n' + call;
+    // Patch: 컴시간 웹사이트 JS가 numberPart 미정의 변수 참조하는 버그 회피
+    const stubs = 'var numberPart = typeof numberPart !== "undefined" ? numberPart : function(s){return s;};\n';
+    const script = stubs + codeConfig.script + '\n\n' + call;
 
     /** DEAD: Sorry about using eval() **/
     const res = eval(script);
